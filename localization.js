export class Hdr {
    static menuItems(isEng) {
        return (isEng ? ['Home',  'About',  'Explore']
        : ['Početna',  'O nama',  'Istraži'])
    }
}

export class Home {
    static youSuck(isEng) {
        return (isEng ? 'YOU SUCK AT DESIGN.'
        : 'LOŠ SI U DIZAJNU.')
    }
    static letsFixThat(isEng) {
        return (isEng ? "Let's fix that"
        : "Idemo to popraviti")
    }
    static imReady(isEng) {
        return (isEng ? "I'm ready"
        : 'Spreman sam')
    }
}
export class Abt {
    static about(isEng) {
        return (isEng ? 'About.'
        : 'O nama.')
    }
    static weMakeItARule(isEng) {
        return (isEng ? "We make it a rule never to criticize something unless we can offer a solution."
        : "Naše je pravilo nikad kritizirati nešto osim ako ne možemo ponuditi rješenje.")
    }
    static thisWebpageContains(isEng) {
        return (isEng ? "This webpage contains several important design principles  powerful tools for designers to ensure that their products are understandable and usable"
        : 'Ova stranica sadrži neke važne principe dizajna  moćne alate za dizajnere koji osiguravaju da je njihov proizvod razumljv i iskoristiv')
    }
    static exploreHow(isEng) {
        return (isEng ? "Explore how" 
         : "Istraži kako")
    }
}
export class Exp {
    static craps(isEng) {
        return (isEng ? ['Contrast',  'Repetition',  'Alignment',  'Proximity'] 
         : ['Kontrast',  'Ponavljanje',  'Poravnanje',  'Blizina'])
    }
}
export class Log {
    static login(isEng) {
        return (isEng ? 'Login' 
         : 'Prijavi se')
    } 
    static loginAlert(isEng) {
        return (isEng ? 'One day I will log you in on correct credentials :)' 
         : 'Jednoga dana moć ću te ulogirati sa ispravnim podatcima :)')
    } 
    static google(isEng) {
        return (isEng ? 'One day I will be a Google signup popup :]' 
         : 'Jednoga dana bit ću Google popup za prijavljivanje' )
    } 
    static facebook(isEng) {
        return (isEng ? 'One day I will be a Facebook signup popup :]' 
         : 'Jednoga dana bit ću Facebook popup za prijavljivanje' )
    } 
    static apple(isEng) {
        return (isEng ? 'One day I will be an Apple signup popup :]' 
         : 'Jednoga dana bit ću Apple popup za prijavljivanje' )
    }
}
export class Crap {
    static goBack(isEng) {
        return (isEng ? 'Go back' 
         : 'Natrag')
    } 
    static break(isEng) {
        return (isEng ? 'Break' 
         : 'Uništi')
    } 
    static fix(isEng) {
        return (isEng ? 'Fix' 
         : 'Popravi')
    }
}
export class Contr {
    static contrast(isEng) {
        return (isEng ? 'Contrast.' 
         : 'Kontrast.')
    } 
    static contrastTextUpper(isEng) {
        return (isEng ? "Contrast is all about making distinct elements stand out and is used to drive a user’s attention to specific elements in a design." 
         : "Kontrast je usmjeren na izdvajanje različitih elemenata i koristi se za privlačenje pažnje korisnika na određene elemente u dizajnu")
    } 
    static colorTitle(isEng) {
            return (isEng ? 'Color.' 
             : 'Boja.')
        } 
    static colorText1(isEng) {
            return (isEng ? "Color contrast is one of the most fundamental design principles that most of us are familiar with. Even so  its application can still prove to be tricky in some cases." 
             : "Kontrast boja jedno je od najosnovnijih načela dizajna s kojima je većina nas poznata. Unatoč tome  njegova se primjena u nekim slučajevima može pokazati teškom.")
        } 
    static colorText2(isEng) {
        return (isEng ? 'As a thumb-rule  high contrast is required between the text and the background colors. However  having contrasting colors for multiple elements can make a design look messy sometimes.' 
            : 'U principu  potreban je visok kontrast između teksta i boja pozadine. Međutim  kontrastne boje za više elemenata mogu učiniti da dizajn ponekad izgleda neuredno.')
    } 
    static colorText3(isEng) {
        return (isEng ? 'Moreover  contrast is not just about using complementary colors but you must also ensure that participating colors in a design don’t strain users’ eyes.' 
            : 'Štoviše  kontrast nije samo korištenje komplementarnih boja  već moraš osigurati da boje koje sudjeluju u dizajnu ne naprežu oči korisnika.')
    }
    static sizeTitle(isEng) {
            return (isEng ? 'Size.' 
             : 'Veličina.')
        } 
    static sizeText1(isEng) {
            return (isEng ? "Contrast can be maintained between discrete elements  especially text  using different sizes." 
             : "Kontrast se može održavati između različitih elemenata  posebno teksta  korištenjem različitih veličina.")
        } 
    static sizeText2(isEng) {
            return (isEng ? "To draw users’ attention towards a certain element  keep its size significantly larger than the surrounding elements." 
             : 'Kako bi privukao pozornost korisnika na određeni element  napravi da njegova veličina bude znatno veća od okolnih elemenata.')
        } 
    static sizeExamples(isEng) {
            return (isEng ? ['Heading 1', 'Heading 2', 'paragraph'] 
             : ['Naslov',  'Podnaslov',  'odlomak'])
        }
    }
export class Repet { 
    static repetition(isEng) {
        return (isEng ? 'Repetition.' 
         : 'Ponavljanje.')
    } 
    static repetitionTextUpper(isEng) {
        return (isEng ? "Repetition is how you maintain consistency in a design. It helps users get familiarized with the way information is presented to them." 
         : "Ponavljanje je način na koji održavaš dosljednost u dizajnu. Pomaže korisnicima da se upoznaju s načinom na koji im se informacije prezentiraju.")
    } 
    static text1(isEng) {
        return (isEng ? 'Repetition of elements is what gives an identity to a design.' 
         : 'Ponavljanje elemenata ono je što dizajnu daje identitet.')
    } 
    static text2(isEng) {
        return (isEng ? 'It can be practiced with the colors  shapes  textures  sizes  and other attributes of the elements in a design.' 
         : 'Može se prakticirati s bojama  oblicima  teksturama  veličinama i drugim atributima elemenata u dizajnu.')
    } 
    static alert(isEng) {
        return (isEng ? "I'm here just for demonstration purposes :)" 
         : 'Tu sam samo za potrebe demonstracije :)')
    } 
    static buttonTitle(isEng) {
        return (isEng ? 'Button' 
         : 'Gumb')
    } 
    static text3(isEng) {
        return (isEng ? 'Your website should use same font types and sizes. The images used should be of same styles. With too many different-looking things on a web page  it will not look like everything belongs on the same page - it will not look cohesive.' 
         : 'Tvoja web stranica treba koristiti iste vrste i veličine fonta. Korištene slike trebaju biti istog stila. S previše stvari različitog izgleda na web stranici  neće izgledati kao da sve pripada istoj stranici - neće izgledati kohezivno.')
    }
}
export class Align { 
    static alignment(isEng) {
        return (isEng ? 'Alignment.' 
         : 'Poravnanje.')
    } 
    static alignmentTextUpper1(isEng) {
        return (isEng ? "Nothing should be placed on the page arbitrarily." 
         : "Ništa ne bi trebalo proizvoljno stavljati na stranicu.")
    } 
    static alignmentTextUpper2(isEng) {
        return (isEng ? "Every item should have a visual connection with something else on the page." 
         : "Svaka stavka treba imati vizualnu vezu s nečim drugim na stranici.")
    } 
    static text1(isEng) {
        return (isEng ? 'Straight lines give a more organized appearance' 
         : 'Ravne linije daju organiziraniji izgled')
    } 
    static text2(isEng) {
        return (isEng ? 'Even when aligned elements are physically separated from  each other  there is an invisible line that connects them  both in your eye and in your mind' 
         : 'Čak i kada su poravnati elementi fizički odvojeni jedan od drugog  postoji nevidljiva linija koja ih povezuje  i u tvom oku i u tvom umu')
    } 
    static text3(isEng) {
        return (isEng ? 'As a general rule  left and right alignment work best – particularly for paragraphs of text. Center alignment is terrible for paragraphs  but can work for things other than text' 
         : 'U pravilu  lijevo i desno poravnanje najbolje funkcioniraju – osobito za odlomke teksta. Centralno poravnanje je strašno za odlomke  ali može proći za stvari osim teksta')
    } 
}
export class Prox {
    static proximity(isEng) {
        return (isEng ? 'Proximity.' 
         : 'Blizina.')
    } 
    static alignmentTextUpper1(isEng) {
        return (isEng ? "Things that are related should be grouped close together" 
         : "Stvari koje su povezane trebale bi biti grupirane zajedno")
    } 
    static alignmentTextUpper2(isEng) {
        return (isEng ? "Things that are not related should be separated" 
         : "Stvari koje nisu povezane treba razdvojiti")
    } 
    static brokenText(isEng) {
        return (isEng ? 'When several items are in close proximity to each other  they become one visual unit rather than several separate units. This helps organize information  reduces clutter  and gives the content consumer a clear structure. How to get it? Count the number of visual elements (units) on the page by counting the number of times you eye stops. If more than some reasonable number  see which elements can be grouped toghether into closer proximity to become one visual unit. The basic purpose of proximity is to organize content' 
         : 'Kada je nekoliko stavki u neposrednoj blizini jedna drugoj  one postaju jedna vizualna jedinica  a ne nekoliko odvojenih jedinica. To pomaže organizirati informacije  smanjuje nered i daje korisniku sadržaja jasnu strukturu. Kako ga postići? Izbroji broj vizualnih elemenata (jedinica) na stranici brojeći koliko puta ti se pogled zaustavi. Ako je više od razumnog broja  pogledaj koji se elementi mogu grupirati kako bi postali jedna vizualna jedinica. Osnovna svrha blizine je organiziranje sadržaja')
    } 
    static text1(isEng) {
        return (isEng ? 'When several items are in close proximity to each other  they become one visual unit rather than several separate units' 
         : 'Kada je nekoliko stavki u neposrednoj blizini jedna drugoj  one postaju jedna vizualna jedinica  a ne nekoliko odvojenih jedinica')
    } 
    static text2(isEng) {
        return (isEng ? 'This helps organize information  reduces clutter  and gives the content consumer a clear structure' 
         : 'To pomaže organizirati informacije  smanjuje nered i daje korisniku sadržaja jasnu strukturu')
    } 
    static text3(isEng) {
        return (isEng ? 'Count the number of visual elements (units) on the page by counting the number of times you eye stops' 
         : 'Izbroji broj vizualnih elemenata (jedinica) na stranici brojeći koliko puta ti se pogled zaustavi')
    } 
    static text4(isEng) {
        return (isEng ? 'If more than some reasonable number  see which elements can be grouped toghether into closer proximity to become one visual unit' 
         : 'Ako je više od razumnog broja  pogledaj koji se elementi mogu grupirati kako bi postali jedna vizualna jedinica')
    } 
    static h2_1(isEng) {
        return (isEng ? 'How to get it?' 
         : 'Kako ga postići? ')
    } 
    static h2_2(isEng) {
        return (isEng ? 'The basic purpose of proximity is to organize content' 
         : 'Osnovna svrha blizine je organiziranje sadržaja')
    }
}